# Data Directory

- `raw/` and `processed/` folders are for local data only. **Do not commit** their contents to version control.
- `kg/` contains knowledge graph data, including snapshots and a changelog.
